# Ticket Backlog

Tickets are small work packages that can be implemented independently.

Owner roles:

- `Frontend`
- `Backend`
- `Offline/Partnership`
- `QA/Release`

Status values:

- `open`
- `in-progress`
- `review`
- `done`

Each ticket should explain what the step is for, what it changes, and how to verify it.
